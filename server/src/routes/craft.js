/**
 * Rota: GET /api/craft/:itemId
 * Query params: server, location
 * Calcula custo de craft: busca receita no ao-bin-dumps, consulta preços dos
 * materiais na AODB e retorna breakdown de custo vs. preço de venda.
 */
