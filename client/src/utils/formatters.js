/**
 * Funções utilitárias de formatação.
 * - formatSilver(value): formata valores em silver (ex: 1.234.567)
 * - formatDate(isoString): formata datas para exibição PT-BR
 * - formatPercentage(value): formata percentuais de lucro
 */
